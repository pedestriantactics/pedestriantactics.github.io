HOW TO USE

Every file that allows easy customization of the website has "user-" at the beginning of the name.

The title and links can be customized in the _data/user-config.yml file.

The colors and font can be customized in the user-styles.css file.

The favicon (which is shown when someone bookmarks the website) can be changed by replacing user-favicon.png with a square image of your choice. I recommend an image no larger than 512px square.

An image can be used as the logo by replacing user-image.png with your logo and setting show-image to false in user-config.yml. Alternatively you can use a pic of yourself or your cat and put it above or below the title. 

A background image can be used by setting user-background to true in user-settings.yml and replacing user-background.png with your favorite image (maybe of cat fur).

CREDITS
Jekyll: https://jekyllrb.com
Fitty: https://github.com/rikschennink